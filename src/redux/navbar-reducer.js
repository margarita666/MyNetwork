let initialState = {friends: [
  {id: 1, name:'Helen'},
  {id: 2, name:'Julia'},
  {id: 3, name:'Inna'}
]}

let navbarReducer = (state= initialState, action) => {
  return state
}

export default navbarReducer;